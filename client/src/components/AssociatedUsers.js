import React, { useContext } from "react";
import { UserContext } from "./Context";

function AssociatedUsers(){
    const {user} = useContext(UserContext)

   // const associatedUsersCards = user.companies

    return(
        <div>

        </div>
    )
}

export default AssociatedUsers